<div class="gradient-background">
  <div class="blur-circle blur-circle-1"></div>
  <div class="blur-circle blur-circle-2"></div>
  <div class="blur-circle blur-circle-3"></div>
</div>

<style>
  .gradient-background {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    overflow: hidden;
    z-index: -1;
    opacity: 0.7;
  }

  .blur-circle {
    position: absolute;
    border-radius: 50%;
    filter: blur(70px);
  }

  .blur-circle-1 {
    width: 40vw;
    height: 40vw;
    background: radial-gradient(circle, rgba(139, 92, 246, 0.3) 0%, rgba(67, 56, 202, 0.1) 70%);
    top: -10%;
    right: -5%;
    animation: float 20s ease-in-out infinite alternate;
  }

  .blur-circle-2 {
    width: 35vw;
    height: 35vw;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.2) 0%, rgba(16, 185, 129, 0.1) 70%);
    bottom: -10%;
    left: -5%;
    animation: float 15s ease-in-out infinite alternate-reverse;
  }

  .blur-circle-3 {
    width: 30vw;
    height: 30vw;
    background: radial-gradient(circle, rgba(239, 68, 68, 0.15) 0%, rgba(251, 146, 60, 0.07) 70%);
    top: 40%;
    right: 20%;
    animation: float 25s ease-in-out infinite alternate;
  }

  @keyframes float {
    0% {
      transform: translate(0, 0);
    }
    100% {
      transform: translate(3%, 3%);
    }
  }

  /* Adjust opacity for dark mode */
  :global(.dark) .gradient-background {
    opacity: 0.5;
  }
</style> 
