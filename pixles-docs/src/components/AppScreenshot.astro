<div class="screenshot-container">
  <div class="screenshot-wrapper">
    <div class="screenshot-header">
      <div class="controls">
        <span class="control"></span>
        <span class="control"></span>
        <span class="control"></span>
      </div>
    </div>
    <div class="screenshot-content">
      <img src="/pixles-screenshot.webp" alt="Pixles application interface" class="screenshot-img" />
    </div>
  </div>
</div>

<style>
  .screenshot-container {
    width: 100%;
    max-width: 1000px;
    margin: 2rem auto;
    padding: 0 1rem;
  }

  .screenshot-wrapper {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    background-color: var(--sl-color-gray-6);
    border: 1px solid var(--sl-color-gray-5);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .screenshot-wrapper:hover {
    transform: translateY(-5px);
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
  }

  .screenshot-header {
    padding: 0.75rem 1rem;
    background-color: var(--sl-color-gray-7);
    border-bottom: 1px solid var(--sl-color-gray-5);
    display: flex;
  }

  .controls {
    display: flex;
    gap: 0.5rem;
  }

  .control {
    width: 12px;
    height: 12px;
    border-radius: 50%;
    display: inline-block;
  }

  .control:nth-child(1) {
    background-color: #ff5f57;
  }

  .control:nth-child(2) {
    background-color: #ffbd2e;
  }

  .control:nth-child(3) {
    background-color: #28c840;
  }

  .screenshot-content {
    width: 100%;
    overflow: hidden;
    position: relative;
    background-color: var(--sl-color-black);
  }

  .screenshot-img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* For dark mode, adjust shadow */
  :global(.dark) .screenshot-wrapper {
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  }

  :global(.dark) .screenshot-wrapper:hover {
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
  }
</style>

<!-- Note: Make sure to place a screenshot image at public/pixles-screenshot.webp --> 
