# Pixles Sync API

The sync API is used to bulk synchronize metadata. It uses gRPC to support efficient full and incremental synchronization. Since metadata is primarily small but high-quantity, API is optimized for low-latency, data-efficient usage.
