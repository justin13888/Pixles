# Pixles Metadata API

The metadata API is used to store and retrieve metadata about assets. It primarily uses gRPC and WebSockets via Protobuf for communication. Since metadata is primarily small but high-quantity, API is optimized for low-latency, data efficient usage.
